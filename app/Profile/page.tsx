import ProfilePageContent from "@/components/AccountPage/ProfilePageContent";

export default function Profile() {

    // I'm waving the white flag for this part of the 
    // project, atleast for this class, I got really close
    // but I don't think I had time to incoporate an entire 
    // profile system in just a day haha

    // Registration works just click login on the account page 
    // with your desired info and itll go straight to the database 
    // mongodb

    return (
        <>
            <ProfilePageContent/>
        </>
    );
}